apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "digitfy.fullname" . }}-test-connection"
  labels:
    {{- include "digitfy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "digitfy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
