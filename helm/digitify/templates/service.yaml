apiVersion: v1
kind: Service
metadata:
  name: {{ include "digitfy.fullname" . }}
  labels:
    {{- include "digitfy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digitfy.selectorLabels" . | nindent 4 }}
